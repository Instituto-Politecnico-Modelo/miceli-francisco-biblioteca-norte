class Evento {
    constructor(id, titulo, fecha, descripcion, capacidad) {
        this.id = id;
        this.titulo = titulo;
        this.fecha = fecha;
        this.descripcion = descripcion;
        this.capacidad = capacidad;
    }
}

module.exports = Evento;